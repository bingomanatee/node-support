/**
 * note - this is an incomplete library of support's most common libs.
 */

module.exports = {
    gate: require('./gate'),
    Gate: require('./gate'),
    mongoose_model: require('./mongoose_model'),

    proper_path: require('./proper_path'),

    pipe: require('./pipe')

}